# Leetcode CN 977.SortedSquares
# https://leetcode.cn/problems/squares-of-a-sorted-array/
# First solution: Violent 

class Solution:
    def sortedSquares(self, nums: List[int]) -> List[int]:
        for i in range(len(nums)):
            nums[i] *= nums[i]
        return sorted(nums)
